import React from "react";

const EmailTemplate = ({ message }) => {
  return (
    <div
      className="text-xl bg-teal-700
  "
    >
      {message}
    </div>
  );
};

export default EmailTemplate;
